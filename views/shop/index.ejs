<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/product.css">
</head>

<body>
    <%- include('../includes/navigation.ejs') %>
    <main>
        <div class="grid">
            <div class="card__actions">
                <h1><a href="/xac-nhan-lam-viec/?active=true" class="btn">ĐIỂM DANH</a></h1> 
            </div>
            <div class="card__actions">
                <h1><a href="/xac-nhan-ket-thuc-lam" class="btn">KẾT THÚC</a></h1> 
            </div>
            <div class="card__actions">
                <h1><a href="/xin-nghi-phep" class="btn">NGHỈ PHÉP</a></h1>
            </div>
        </div>
        <% for (let prod of result) { %>
            <% if(prod.status) { %>
                <div class="grid">
                    <div class="card__actions">
                        <h2>StartTime: <%= prod.startTime %></h2>
                        <h2>Place: <%= prod.place %></h2>
                        <h2>Active:</h2>
                    </div>
                </div>
            <% } %>
        <% } %>
    </main>
    
<%- include('../includes/end.ejs') %>